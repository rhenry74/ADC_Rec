<UserControl
    x:Class="ADC_Rec.Controls.Filters.DelayControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ADC_Rec.Controls.Filters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="150"
    d:DesignWidth="300">
    <StackPanel Spacing="8">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Delay (ms):" />
                <TextBlock Text="{x:Bind ViewModel.DelayTimeMs, Mode=OneWay}" Grid.Column="1" HorizontalAlignment="Right" />
            </Grid>
            <Slider Minimum="10" Maximum="2000" Value="{x:Bind ViewModel.DelayTimeMs, Mode=TwoWay}" />
        </StackPanel>
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Feedback:" />
                <TextBlock Text="{x:Bind ViewModel.Feedback, Mode=OneWay}" Grid.Column="1" HorizontalAlignment="Right" />
            </Grid>
            <Slider Minimum="0" Maximum="0.9" Value="{x:Bind ViewModel.Feedback, Mode=TwoWay}" StepFrequency="0.1" />
        </StackPanel>
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Wet Level:" />
                <TextBlock Text="{x:Bind ViewModel.WetLevel, Mode=OneWay}" Grid.Column="1" HorizontalAlignment="Right" />
            </Grid>
            <Slider Minimum="0" Maximum="1" Value="{x:Bind ViewModel.WetLevel, Mode=TwoWay}" StepFrequency="0.1" />
        </StackPanel>
    </StackPanel>
</UserControl>
