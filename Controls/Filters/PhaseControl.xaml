<UserControl
    x:Class="ADC_Rec.Controls.Filters.PhaseControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ADC_Rec.Controls.Filters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="150"
    d:DesignWidth="300">
    <StackPanel Spacing="8" Margin="8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Sync Delay (ms):" />
            <TextBlock Text="{x:Bind ViewModel.DelayTimeMs, Mode=OneWay}" Grid.Column="1" HorizontalAlignment="Right" />
        </Grid>
        <Slider Minimum="0" Maximum="1000" Value="{x:Bind ViewModel.DelayTimeMs, Mode=TwoWay}" StepFrequency="1" />
    </StackPanel>
</UserControl>
